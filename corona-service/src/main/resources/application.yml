spring:
  application:
    name: corona-service
server:
  port: 8081
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8083/eureka/
  instance:
    hostname: localhost
    